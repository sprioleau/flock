/**
 * @tandem/email-sdk — document definition, Zod schemas, operations, renderers.
 *
 * Phase 1 builds this out (see docs/email-editor-phased-plan.md §5, Phase 1).
 * This stub exists so the workspace wiring (apps/web, convex/) can import it
 * from day one.
 */

export const SDK_VERSION = "0.1.0";
